# ──────────────────────────────────────────────────
# CHM_KRYPTON — Environment Variables Example
# Скопируй этот файл в .env и заполни свои значения
# ──────────────────────────────────────────────────

# ── App ──────────────────────────────────────────
APP_ENV=dev
APP_DEBUG=true
APP_SECRET_KEY=your-256-bit-random-secret-key-here

# ── Database (PostgreSQL) ─────────────────────────
# Можно задать по компонентам ИЛИ через DATABASE_URL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_NAME=chm_krypton
# DATABASE_URL=postgresql://user:pass@host:5432/chm_krypton

# ── Redis ─────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_URL=redis://localhost:6379/0

# ── Telegram ──────────────────────────────────────
TELEGRAM_BOT_TOKEN=1234567890:AAAA-your-bot-token-here
TELEGRAM_BOT_USERNAME=chm_krypton_bot
TELEGRAM_WEBAPP_URL=https://your-frontend-domain.com
SUPER_ADMIN_TG_ID=123456789

# ── Bybit API ─────────────────────────────────────
# Master-аккаунт (создаёт суб-аккаунты, переводит средства)
BYBIT_MASTER_API_KEY=your-bybit-master-api-key
BYBIT_MASTER_API_SECRET=your-bybit-master-api-secret
BYBIT_MASTER_MIN_BALANCE=10000

# Базовые URL (менять только при тестировании)
BYBIT_DEMO_BASE_URL=https://api-demo.bybit.com
BYBIT_REAL_BASE_URL=https://api.bybit.com

# ── JWT ───────────────────────────────────────────
JWT_SECRET=your-jwt-secret-256-bit-random-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# ── AES-256 шифрование API ключей ────────────────
# Обязательно: 64 hex-символа (= 32 байта)
AES_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ── Реферальная система ───────────────────────────
REFERRAL_LEVEL1_PCT=10.0
REFERRAL_LEVEL2_PCT=3.0
REFERRAL_PAYOUT_DAYS=7

# ── Выплаты ───────────────────────────────────────
MIN_PAYOUT_AMOUNT=50.0

# ── Rate Limiting ─────────────────────────────────
RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_TRADING_PER_MINUTE=10

# ── Challenge Engine ──────────────────────────────
ENGINE_CHECK_INTERVAL_SECONDS=30

# ── Frontend (Vite) ───────────────────────────────
VITE_API_URL=https://your-backend-domain.com/api/v1
VITE_WS_URL=wss://your-backend-domain.com/ws
VITE_BOT_USERNAME=chm_krypton_bot
